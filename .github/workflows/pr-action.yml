on:
  push:
    branches:
      - bleh
    paths:
      - "src/package/index.html"

jobs:
  pull-request:
    runs-on: ubuntu-latest
    outputs:
      file: ${{ steps.changes.outputs.file }}
    steps:
      - name: Local Checkout
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Get changed files
        id: changes
        # Set outputs using the command.
        run: |
          echo "::set-output name=file::$(git diff --no-commit-id --name-only -r ${{ github.sha }} | xargs)"
      - name: Remote Repo Checkout
        uses: actions/checkout@v3
        with:
          repository: calitaz/tibia_discord_bot
          token: ${{ secrets.BOT }}
      - name: Print
        run: |
          echo ${{ steps.changes.outputs.file }}
      - name: Make changes to pull request
        run: date +%s > report.txt
      # - name: Pull-Request
      #   uses: peter-evans/create-pull-request@v4
      #   with:
      #     title: "[AUTO] - Update `index.html`"
      #     commit-message: Update index
      #     committer: GitHub <noreply@github.com>
      #     author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
      #     body: |
      #       Update report
      #       - `index.html` was updated, please update your index.html
      #       - Auto-generated by [create-pull-request][1]

      #       [1]: https://github.com/peter-evans/create-pull-request
      #     draft: true
      #     labels: |
      #       automated pr
      #     branch: auto-pr/update-index.html
      #     token: ${{ secrets.BOT }}
      #     base: main
